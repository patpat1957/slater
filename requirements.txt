# ─────────────────────────────────────────────────────────────
#  Lotto Extraction API v2.0 — Production Dependencies
#  Includes: REST API + ML Prediction Engine
# ─────────────────────────────────────────────────────────────

# ── Core API Framework ──
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
httpx>=0.27.0,<1.0
pydantic>=2.6.0,<3.0
requests>=2.31.0,<3.0
beautifulsoup4>=4.12.0,<5.0
lxml>=5.0.0,<6.0
slowapi>=0.1.9,<1.0

# ── ML Dependencies (Machine Learning Engine) ──
numpy>=1.26.0,<2.0
pandas>=2.2.0,<3.0
scikit-learn>=1.4.0,<2.0
joblib>=1.3.0,<2.0
python-multipart>=0.0.7        # FastAPI file uploads

# ── Optional ML Libraries (enhanced models) ──
# Uncomment for full ensemble support:
# xgboost>=2.0.0,<3.0
# lightgbm>=4.3.0,<5.0

# ── Production Server ──
gunicorn>=22.0.0,<23.0         # Production WSGI/ASGI server
